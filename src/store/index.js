import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

const state = {

};

const actions = {

};

const mutations = {

};

const store = new Vuex.Store({
  state,
  actions,
  mutations,
  modules: [
  ],
});

export default store;
